package de.dopebrot.dpu;

public class MathHelper {

    // DPB Math


    public Integer max(Integer current, Integer max) {
        if (current > max) {
            current = max;
        }
        return current;
    }

    public Integer min(Integer current, Integer min) {
        if (current < min) {
            current = min;
        }
        return current;
    }


    public Double max(Double current, Double max) {
        if (current > max) {
            current = max;
        }
        return current;
    }

    public Double min(Double current, Double min) {
        if (current < min) {
            current = min;
        }
        return current;
    }

    public Float max(Float current, Float max) {
        if (current > max) {
            current = max;
        }
        return current;
    }

    public Float min(Float current, Float min) {
        if (current < min) {
            current = min;
        }
        return current;
    }

    public Short max(Short current, Short max) {
        if (current > max) {
            current = max;
        }
        return current;
    }

    public Short min(Short current, Short min) {
        if (current < min) {
            current = min;
        }
        return current;
    }

    public Byte max(Byte current, Byte max) {
        if (current > max) {
            current = max;
        }
        return current;
    }

    public Byte min(Byte current, Byte min) {
        if (current < min) {
            current = min;
        }
        return current;
    }

}
